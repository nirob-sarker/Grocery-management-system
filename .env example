PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=gms

JWT_SECRET=change_me
JWT_EXPIRES=7d

SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=xxxx
SMTP_PASS=xxxx
MAIL_FROM="GMS <no-reply@gms.com>"

LOW_STOCK_THRESHOLD=5
